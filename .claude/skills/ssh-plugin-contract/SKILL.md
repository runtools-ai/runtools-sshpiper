---
name: ssh-plugin-contract
description: Compatibility pointer. Use the canonical ssh-plugin-contract skill in .agents/skills/ssh-plugin-contract/SKILL.md.
---

# ssh-plugin-contract

Canonical skill: `.agents/skills/ssh-plugin-contract/SKILL.md`.

Do not duplicate the full instructions here. `.agents/skills/` is the RunTools source of truth; this `.claude` copy exists only for older launchers that still discover skills from `.claude/skills/`.
